# 서버 헬스체크 환경 변수 설정
# 이 파일을 .env.local로 복사하고 값을 설정하세요
# cp env.example .env.local

# 모니터링할 GraphQL 엔드포인트 목록 (필수)
# 쉼표로 구분하거나 JSON 배열 형식으로 설정 가능
HEALTH_CHECK_ENDPOINTS=https://example.com,https://example1.com

# 또는 JSON 배열 형식:
# HEALTH_CHECK_ENDPOINTS=["https://dev-api.athletec.co.kr/graphql","https://field-link-api.athletec.co.kr/graphql"]

# 슬랙 웹훅 URL (서버 사이드에서만 접근 가능)
# 슬랙 알림을 받으려면 이 값을 설정해야 합니다
SLACK_WEBHOOK_URL=

# 슬랙 웹훅 URL 설정 방법:
# 1. https://api.slack.com/apps 에서 새 앱 생성
# 2. "Incoming Webhooks" 기능 활성화
# 3. 웹훅 URL을 생성하고 복사
# 4. 아래에 붙여넣기

# Next.js 기본 URL (선택사항, 프로덕션 배포 시 필요할 수 있음)
# NEXT_PUBLIC_BASE_URL=http://localhost:3000

